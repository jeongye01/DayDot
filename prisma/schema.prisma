// schema.prisma

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model User {
  id        String   @id @default(cuid())  // 실제 userId (랜덤 UUID or cuid)
  email     String?  @unique               // ✅ nullable → 로그인 붙이면 사용
  name      String?                        // 선택 사항
  image     String?                        // 구글 프로필 이미지 (추후)
  entries   Entry[]
  createdAt DateTime @default(now())
}


model Entry {
  id        String   @id @default(cuid())
  content   String?  
  mood      Mood
  date      DateTime
  user      User     @relation(fields: [userId], references: [id])
  userId    String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

enum Mood {
  LOVE      // 😍
  HAPPY     // 😊
  NEUTRAL   // 😐
  SAD       // 😞
  ANGRY     // 😡
}
